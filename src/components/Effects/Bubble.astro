<div class="container">
  <div class="bubblewrap">
    <div class="bubble Left"></div>
    <div class="bubble Left"></div>
    <div class="bubble Left"></div>
    <div class="bubble Right"></div>
    <div class="bubble Right"></div>
    <div class="bubble Right"></div>
  </div>
</div>

<style lang="scss">
  .container {
    height: 27.3333333333vw;
    min-width: 100vw;
    position: absolute;
    right: 0;
    bottom: 10vh;
  }

  .bubblewrap {
    bottom: 15%;
    width: 6vw;
    height: 6vw;
    position: absolute;
    right: 0vw;
  }

  .bubble {
    position: absolute;
    backface-visibility: hidden;
    perspective: 1000;
    margin: 3px;
    border-radius: 50%;
    background: radial-gradient(
      circle,
      rgba(255, 171, 0, 1) 50%,
      rgba(255, 131, 55, 1) 100%
    );
    opacity: 0;
  }

  @for $i from 1 through 6 {
    $widthRandom: random(8) + $i;
    $rightRandom: ($i * 0.5)+2.7;
    $leftRandom: ($i * 0.5)+1.8;

    .Right:nth-child(#{$i}) {
      bottom: 40%;
      right: $rightRandom + vw;
      width: calc(0.06 * $widthRandom) + vw;
      height: calc(0.06 * $widthRandom) + vw;
      animation: bubbleUp random(2000) + 1300ms infinite linear;
      animation-delay: random(2100) + 2000ms;
      box-shadow: inset calc(0.022 * $widthRandom) + vw 0 rgba(255, 131, 55, 1);
    }

    .Left:nth-child(#{$i}) {
      bottom: 32%;
      right: $leftRandom + vw;
      width: calc(0.05 * $widthRandom) + vw;
      height: calc(0.05 * $widthRandom) + vw;
      animation: bubbleUp random(2000) + 3300ms infinite linear;
      animation-delay: random(2900) + 1000ms;
      box-shadow: inset calc(0.018 * $widthRandom) + vw 0 rgba(255, 131, 55, 1);
    }
  }

  @keyframes bubbleUp {
    0% {
      transform: translate3d(0, 0, 0) scale(1) rotate(32deg);
      opacity: 0.9;
    }
    25% {
      transform: translate3d(2px, -0.15vw, 0) scale(1.1) rotate(32deg);
    }
    75% {
      transform: translate3d(-2px, -0.45vw, 0) scale(1.3) rotate(32deg);
    }
    95% {
      transform: translate3d(0px, -0.6vw, 0) scale(1.4) rotate(32deg);
      opacity: 0.9;
    }
    100% {
      transform: translate3d(5px, -0.6vw, 0) scale(3) rotate(32deg);
      opacity: 0;
    }
  }

  @media screen and (max-width: 768px) {
    .container {
      display: none;
    }
  }
</style>
